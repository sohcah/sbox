# Host directory mounts — implementation plan

**Superseded** by [`host-mounts-plan.md`](./host-mounts-plan.md).

Host mounts (`mounts:`) cover files and directories; kind is inferred at create.
This document remains only as a pointer for older links.
